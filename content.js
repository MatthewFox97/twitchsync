// content.js
// This script runs on Twitch pages.

// console.log("Twitch Sync content script loaded.");

// Future functionality could be added here, such as:
// - More robust video player detection.
// - Listening for messages from the popup or background script.
// - Extracting stream latency information (if possible).

// For now, the core delay logic is injected via chrome.scripting.executeScript
// from popup.js, which directly accesses the video element.
